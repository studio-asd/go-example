package api

import "context"

const (
	// DO NOT CHANGE. SecretKeyUserPassword is the key to identify the user password inside the
	// secret storage for all users.
	secretKeyUserPassword = "user_password"
)

func (a *API) createSecret(ctx context.Context) {
}
